const {ethers} = require("hardhat");

async function main(){
    //get signers
    //deploy AccountFactory
    //call createAccount
    //it returns the initial initCode
    //add factory address with initcode to get actual initCode
    //deploy EntryPoint
    //use try catch of EntryPoint to get sender address

}
main().catch(console.error)